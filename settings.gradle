pluginManagement {
    repositories {
        mavenCentral()
        maven {
            url = uri("https://maven.pkg.github.com/der-fruhling/packmaker")
        }
    }
}

rootProject.name = 'packmaker-template-groovy'
